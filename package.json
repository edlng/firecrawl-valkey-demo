{
  "name": "valkey-demo",
  "version": "1.0.0",
  "description": "Demo showcasing Firecrawl with Valkey for rate limiting, caching, and batch operations",
  "author": "",
  "license": "Apache-2.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "server": "ts-node src/server.ts",
    "server:standalone": "SKIP_DOCKER=true ts-node src/server.ts",
    "benchmark": "ts-node scripts/benchmark-firecrawl.ts",
    "benchmark:compare": "ts-node scripts/benchmark-firecrawl.ts --compare"
  },
  "dependencies": {
    "@valkey/valkey-glide": "^1.3.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "protobufjs": "^7.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
